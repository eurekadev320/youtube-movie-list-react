import { Box, styled } from '@mui/material';
import MenuBackgroundImage from '../../../assets/img-mainNav-tile.png';

const Container = styled(Box)`
  background: url(${MenuBackgroundImage});
`;

export default Container;
